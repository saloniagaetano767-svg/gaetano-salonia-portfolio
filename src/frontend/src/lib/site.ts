/** Central contact email and short nav brand (shown in Navbar / Footer). */
export const CONTACT_EMAIL = "gaetanosalo@outlook.de" as const;

/** Short brand mark for nav / footer (given name + initial). */
export const BRAND_MARK = "S. Gaetano" as const;
